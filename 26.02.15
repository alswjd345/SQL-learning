*charAt()-string으로 저장된 문자열 중에서 한글자만 선택해서 char타입으로 변환해준다

예시)문자열 str이 주어집니다.문자열을 시계방향으로 90도 돌려서 아래 입출력 예와 같이 출력하는 코드를 작성해 보세요.
입력--->abcde
출력->a
	b
	c
	d
	e
    import java.util.Scanner;

    public class Solution {
        public static void main(String[] args) {
            Scanner sc = new Scanner(System.in);
            String a = sc.next();
            for(int i=0;i<a.length();i++){
                System.out.println(a.charAt(i));
            }
        }
    }
*valueOf() 메서드는 괄호 안의 객체를 String 객체로 변환하여 반환
*arrays.sort() 메서드는 오름참순으로 정렬해준다
**Integer.parseInt()메서드는 문자를 숫자로 변환해줌
자바는 배열일 때는 .length를 쓰고, 문자열일 때는 .length()를 써야 한다

문제)문자열 배열 intStrs와 정수 k, s, l가 주어집니다. intStrs의 원소는 숫자로 이루어져 있습니다.
배열 intStrs의 각 원소마다 s번 인덱스에서 시작하는 길이 l짜리 부분 문자열을 잘라내 정수로 변환합니다.
 이때 변환한 정수값이 k보다 큰 값들을 담은 배열을 return 하는 solution 함수를 완성해 주세요.

import java.util.*;

class Solution {
    public int[] solution(String[] intStrs, int k, int s, int l) {
        //배열 만들기 
        List<Integer> list = new ArrayList<>();
            //inStrs를 s만큼 자르기
        for (int i = 0; i < intStrs.length; i++) {
            String sub = intStrs[i].substring(s, s + l);
            //문자열을 숫자형으로 변경
            int num = Integer.parseInt(sub);
            //배열에 넣기
            if (num > k) {
                list.add(num);
            }
        }
        //숫자들을 정해진 형식(배열)으로 옮겨담기
        int[] answer = new int[list.size()];
        for (int i = 0; i < list.size(); i++) {
            answer[i] = list.get(i);
        }
        
        return answer;
    }
}